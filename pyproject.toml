[project]
name = "sandbox-beam"
version = "0.0.0"
requires-python = ">=3.10"
dependencies = ["numpy"]

[project.optional-dependencies]
dev = ["pytest>=7.0", "rainflow>=3.0"]
fatigue = ["rainflow>=3.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
