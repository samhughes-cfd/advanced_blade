# Shared runtime dependencies for `blade_precompute`.
#
# Install:
#   pip install -r requirements.txt
#
# Optional extras live in `requirements-optional.txt`.

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
scikit-image>=0.21
PyYAML>=6.0
